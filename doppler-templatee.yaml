projects:
  - name: csrf-test-project-3
    environments:
      - name: Development
        slug: dev
        configs:
          - slug: dev

    secrets:
      dev:
        TEST_INJECTION: "OVERRIDDEN_BY_ATTACKER"
