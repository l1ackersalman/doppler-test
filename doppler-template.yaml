projects:
  - name: csrf-test-project
    environments:
      - name: Development
        slug: dev
        configs:
          - slug: dev

    secrets:
      dev:
        TEST_INJECTION: "OVERRIDDEN_BY_ATTACKER."
