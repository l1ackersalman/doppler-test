projects:
  - name: csrf-test-project-1
    environments:
      - name: Development
        slug: dev
        configs:
          - slug: dev

    secrets:
      dev:
        TEST_INJECTION: "OVERRIDDEN_BY_ATTACKER"

projects:
  - name: csrf-test-project-2
    environments:
      - name: Development
        slug: dev
        configs:
          - slug: dev

    secrets:
      dev:
        TEST_INJECTION: "OVERRIDDEN_BY_ATTACKER"

