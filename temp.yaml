a: &a "A"
b: &b "*a*a*a*a*a*a*a*a*a*a"

projects:
  - name: 'your-projectt'
    environments:
      - name: 'Development'
        slug: 'dev'
        configs:
          - slug: 'dev'
    secrets:
      dev:
        ENV: *a
